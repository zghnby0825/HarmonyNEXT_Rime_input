name: flypy
author: vgh
version: 1.0.0
description: 方案主题来自 vgh
url: https://github.com/zghnby0825/HarmonyNEXT_Rime_input_VGHFly
create_time: 2023-04-27
update_time: 2023-04-27
# 支持的设备。有指定的只取指定的，没指定的取全部
supported_devices:
  pc: ["vgh-flypy"]
# 方案使用主题
use_theme:
  # 如果没有自定义主题，则使用默认主题.
  # 右边的值是主题的目录名，也可以是方案下的主题名。需要绝对路径。
  #例如: 在方案目录下，rime_ice/themes/default 是默认主题。
  # 在主题目录下，themes/default 是自定义主题。
  default: "flypy/themes/vgh-flypy"
  # 自定义主题
  custom:
    # 自定义主题的名称，路径同上。
    "vgh-flypy": "flypy/themes/vgh-flypy"